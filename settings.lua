data:extend({
  {
    type = "int-setting",
    name = "not-alone-car-minimum-distance",
    setting_type = "runtime-global",
    default_value = 20,
    minimum_value = 20,
    maximum_value = 1000,
    order = "a[not-alone]-a[car-minimum-distance]"
  }
})